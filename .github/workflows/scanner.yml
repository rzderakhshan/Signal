name: Intraday Signal Scanner

on:
  schedule:
    - cron: '*/5 * * * *' # Every 5 minutes, 24/7
  workflow_dispatch: # Manual trigger
    inputs:
      test_telegram:
        description: 'Run connection test (sends 1 Telegram message)'
        type: boolean
        required: false
        default: false

concurrency:
  group: scanner-group
  cancel-in-progress: false # Do not cancel in-progress, let them finish to avoid state corruption

jobs:
  scan:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install Dependencies
        run: pip install -r requirements.txt
        
      - name: Download State (Cache)
        uses: actions/cache/restore@v4
        with:
          path: .state/
          key: scanner-state-${{ github.run_id }}
          restore-keys: |
            scanner-state-
            
      - name: Run Scanner (Dry Run Mode)
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          if [ "${{ github.event.inputs.test_telegram }}" == "true" ]; then
            python scanner.py --test-telegram
          else
            python scanner.py --github-dry-run
          fi

      - name: Save State (Cache)
        uses: actions/cache/save@v4
        if: always()
        with:
          path: .state/
          key: scanner-state-${{ github.run_id }}
